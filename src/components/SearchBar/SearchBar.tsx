import './SearchBar.css';

function SearchBar() {
    return (
        <input
            className="SearchBar"
            placeholder="Series to search for..."
        ></input>
    );
}

export default SearchBar;
